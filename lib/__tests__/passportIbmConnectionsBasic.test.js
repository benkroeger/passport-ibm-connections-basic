'use strict';

const assert = require('assert');
const passportIbmConnectionsBasic = require('../index.js');

describe('passportIbmConnectionsBasic', () => {
  it('has a test', () => {
    assert(false, 'passportIbmConnectionsBasic should have a test');
  });
});
