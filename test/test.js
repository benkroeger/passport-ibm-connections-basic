'use strict';
var assert = require('assert');
var passportIbmConnectionsBasic = require('../');

describe('passport-ibm-connections-basic node module', function () {
  it('must have at least one test', function () {
    passportIbmConnectionsBasic();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
